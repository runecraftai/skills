# loop-contract

Turn the next pending roadmap phase into a scoped `program.md` contract that `loop-run` obeys.
Declares allowed files, BDD acceptance criteria, verify method, risk class, mandatory sub-steps,
and budget — the single source of truth the loop uses for one phase.
